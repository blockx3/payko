function PaymentIssues() {
  // if payment succeed and our system does not acknowledge that payment
  // then user can report on this page
  return <div>payment issues</div>;
}

export default PaymentIssues;
